version: '3.8'

services:
  seq:
    image: datalust/seq:2024.3
    container_name: ecommerce-seq
    environment:
      - ACCEPT_EULA=Y
    ports:
      - "5341:80"
    volumes:
      - ./seq-data:/data
    restart: always
